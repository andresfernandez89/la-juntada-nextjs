import { Inter, Roboto_Mono } from "next/font/google";

export const fontInter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
});

export const fontRoboto_mono = Roboto_Mono({
  subsets: ["latin"],
  variable: "--font-roboto-mono",
});
